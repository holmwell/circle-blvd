// Look for a master config that is located 
// one level above our node_modules folder.
var masterConfig;
try {
    masterConfig = require('../../../config.js');
} 
catch (ex) {
    // I am ok with the events that are 
    // unfolding currently.
}

var databaseName;

if (masterConfig 
    && masterConfig.database 
    && masterConfig.database.name) {
    databaseName = masterConfig.database.name;
}

module.exports = {
    name: databaseName || 'circle-blvd'
};